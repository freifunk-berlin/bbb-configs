plugin: constructed
strict: false
use_vars_plugins: true

groups:
  snmp_target: metrics is defined or snmp_mib is defined

keyed_groups:
  - prefix: snmp_mib
    key: snmp_mib
  - prefix: os
    key: os
  - prefix: target
    key: target
  - prefix: version
    key: openwrt_version|lower
